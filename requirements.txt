# Core pipeline
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
torch>=2.0

# Figures
matplotlib>=3.7
seaborn>=0.12

# Tests
pytest>=7.4

# Optional: the physics-informed penalty in dtaug/physics.py additionally needs
# the MATLAB Engine API for Python, which ships with a MATLAB installation and
# cannot be installed from PyPI.
